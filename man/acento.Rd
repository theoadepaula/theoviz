% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site.R
\name{acento}
\alias{acento}
\title{Acento unico do site}
\usage{
acento(estado = c("base", "hover", "pressionado"))
}
\arguments{
\item{estado}{\code{"base"} (padrao), \code{"hover"} ou \code{"pressionado"}. O \code{hover} e o
de \strong{texto}, que acende; o \code{pressionado} e o de \strong{superficie}, que
afunda. A assimetria e deliberada: fundo que clareia no hover empurra o
rotulo para perto do piso de contraste.}
}
\value{
Uma cor em hexadecimal.
}
\description{
O areia (\verb{#cfae72}), unico acento do sistema visual do site.
}
\section{Quando usar o acento, e quando usar a paleta}{


A regra decide pelo \strong{numero de series}, e nao pelo gosto:

\describe{
\item{\strong{Uma serie}}{use o acento. E o que a figura da home faz, e e o que
faz o grafico ler como peca do site em vez de saida de biblioteca.}
\item{\strong{Duas ou mais}}{use \code{\link[=paleta]{paleta()}}, na ordem dos slots. O acento nao
entra na paleta categorica: ele nao foi validado para daltonismo contra
os quatro slots, e o sistema do site reserva o areia para "acao, estado
ativo ou marcador estrutural".}
}

Nao ha meio-termo: um grafico de tres series com uma delas em areia mistura
dois vocabularios e quebra os dois.
}

\section{A propriedade que este valor tem, e que um acento novo nao herda}{


O areia passa \strong{nos dois sentidos} -- 9,03:1 como texto sobre \code{noite} e os
mesmos 9,03:1 como fundo com rotulo em \code{noite} por cima, porque contraste e
simetrico. E por isso que o site nao tem um "areia de botao" a parte.

Isso e propriedade \strong{deste valor}, nao licenca geral. Um acento que passa
como texto nao passa automaticamente como superficie -- foi exatamente esse
erro que custou uma correcao ao site em 2026-08-04.
}

\examples{
acento()
acento("pressionado")
}
\seealso{
\code{\link[=site]{site()}}, \code{\link[=paleta]{paleta()}}.
}
