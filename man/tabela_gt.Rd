% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabelas.R
\name{tabela_gt}
\alias{tabela_gt}
\title{Tabela no estilo padrao dos artigos}
\usage{
tabela_gt(
  dados,
  titulo = NULL,
  subtitulo = NULL,
  nota = NULL,
  modo = c("claro", "escuro")
)
}
\arguments{
\item{dados}{data.frame ou tibble.}

\item{titulo}{Titulo da tabela (opcional).}

\item{subtitulo}{Linha de apoio abaixo do titulo (opcional).}

\item{nota}{Nota de rodape; aceita markdown (opcional).}

\item{modo}{\code{"claro"} (padrao) ou \code{"escuro"}. Ver a secao acima.}
}
\value{
Um objeto \code{gt_tbl}, encadeavel com as funcoes do pacote gt.
}
\description{
O tema canonico das tabelas \code{gt}: regua horizontal apenas, cabecalho discreto
alinhado a esquerda, nenhuma linha vertical. Coluna se separa por espaco, nao
por tinta.
}
\section{A tabela e o par acessivel do grafico}{


Quando a cor falha -- daltonismo, impressao em preto e branco, tela ruim --
e a tabela que responde. Por isso ela nao e opcional num artigo com figura, e
por isso saida crua de tibble em \verb{<pre>} nao vai ao ar.
}

\section{Escolha o modo pela pagina em que a tabela vai cair}{


O \code{gt} \strong{nao herda} a cor da pagina: sem instrucao ele crava fundo branco com
texto cinza-escuro, inline, e nenhuma folha de estilo do site derruba isso.
Como as paginas do site sao escuras, uma tabela em \code{modo = "claro"} la dentro
e um retangulo branco no meio do texto.

\describe{
\item{\code{"escuro"}}{fundo \strong{transparente} e tintas do site. A tabela pousa na
pagina em vez de recortar uma chapa dentro dela -- a mesma escolha ja
feita nos graficos plotly. E o modo do que vai ao ar.}
\item{\code{"claro"} (padrao)}{superficie clara explicita. Serve impressao, slide
e PDF. Nao e transparente de proposito: transparente com tinta escura por
cima seria ilegivel na pagina escura.}
}

O padrao segue \code{"claro"} pela razao descrita em \code{\link[=tinta]{tinta()}}: trocar o padrao
restilizaria de uma vez artigos ja congelados por \code{freeze}.
}

\section{As duas vozes}{


Colunas numericas saem em \strong{IBM Plex Mono}, o resto em \strong{IBM Plex Sans}.
E a Regra das Duas Vozes do site: sans e discurso, mono e medida. A fronteira
e semantica, nao estetica -- um numero existe para ser comparado com outro, e
a mono e o que diz isso.
}

\examples{
tabela_gt(head(mtcars[, 1:3]), titulo = "Exemplo")

# o que vai ao ar, porque o site e escuro:
tabela_gt(head(mtcars[, 1:3]), titulo = "Exemplo", modo = "escuro")
}
\seealso{
\code{\link[=fmt_br]{fmt_br()}} e \code{\link[=fmt_pct_br]{fmt_pct_br()}} para a formatacao pt-BR das colunas;
\code{\link[=tinta]{tinta()}} para as tintas que este tema consome.
}
